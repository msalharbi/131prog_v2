<div dir="rtl">

# واجب عملي : بناء صفحة ويب بسيطة باستخدام HTML

---

## الهدف من الواجب

تصميم صفحة ويب واحدة (`index.html`) باستخدام العناصر الأساسية في HTML التي أخذناها في المحاضرتين. الواجب بسيط ومباشر، والهدف منه التأكد أنك قادر على كتابة صفحة HTML صحيحة من الصفر بدون نسخ ولصق.

---

## النموذج B — صفحة هوايتي المفضلة (My Favorite Hobby)

أنشئ صفحة باسم `index.html` تتحدث عن هوايتك المفضلة (رياضة، قراءة، طبخ، ألعاب، رسم... أي هواية تختارها)، وتحقق **كل** النقاط التالية:

### 1) الهيكل الأساسي للصفحة
- [ ] تبدأ بـ `<!DOCTYPE html>`
- [ ] تحتوي على `<html>` وبداخله `<head>` و `<body>`
- [ ] داخل `<head>`: وسم `<title>` بعنوان مناسب (مثال: اسم الهواية + "صفحتي المفضلة")
- [ ] اضف خاصية تقوم بتعديل عرض الصفحة لتكون من اليمين لليسار
- [ ] أضف تعليقًا واحدًا على الأقل داخل الكود لتوضيح أي جزء من الصفحة

### 2) العناوين والنصوص
- [ ] عنوان رئيسي واحد `<h1>` باسم الهواية
- [ ] عنوانان فرعيان على الأقل `<h2>` (مثال: "لماذا أحب هذه الهواية"، "نصائح للمبتدئين") — أعطِ كل عنوان `id` خاص به (مثال: `id="why"` و `id="tips"`)
- [ ] فقرتان على الأقل تشرح فيهما الهواية وسبب حبك لها
- [ ] استخدم مرة واحدة على الأقل داخل إحدى الفقرات
- [ ] استخدم للفصل بين قسمين من أقسام الصفحة

### 3) الصورة
- [ ] أضف صورة تمثل الهواية باستخدام

### 4) الروابط التشعبية
- [ ] ضع في أعلى الصفحة (مباشرة بعد `<h1>`) روابط داخلية تنتقل إلى كل قسم من أقسام صفحتك 
- [ ] رابط خارجي لموقع أو قناة لها علاقة بالهواية، يفتح في تبويب جديد

---

## معايير التصحيح (الدرجة النهائية 3 درجات) — النموذج B

| البند | الدرجة |
|---|---|
| الهيكل الأساسي (DOCTYPE, html, head, title, body, ... ) | 1 |
| العناوين والفقرات  | 1 |
| الصورة | 0.5 |
| الروابط | 0.5 |

**ملاحظة:** أي كود لا يعمل (خطأ في الوسوم أو المسارات) يخصم من درجة البند المرتبط به.

---

## طريقة التسليم

1. تأكد أن الصفحة تفتح بشكل صحيح في المتصفح قبل التسليم (افتح `index.html` مباشرة أو عبر Live Server).
2. اضغط مجلد `assignment1` بالكامل (بما فيه مجلد `images`) في ملف واحد بصيغة ZIP.
3. سلّم الملف حسب الطريقة التي يحددها المدرب (منصة التدريب / البريد الإلكتروني).

بالتوفيق!

</div>